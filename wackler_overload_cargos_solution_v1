# В началото ние трябва да знаем, какво е пространството в камионът, както и максимално допустимото претоварване!
room_space = 18 * 120 * 80 #Стандартно приемаме, че простраството в камиона е на база 18 евро палета!
max_overload = 5000 # Стандартното претоварване е до максимум 5000 кг!

#Създаме функция, която пресмята, колко от пространството в камиона ще заеме даден товар!
def cargo_sized(lenght, width):
    return lenght*width

#Въвеждаме номерът на турът, който ще обслужваме.
tour_number = int(input("Въведете номерът на турът, за който въвеждате товарите за денят: "))
#Въвеждаме, колко товари са предвидени да се закарат днес:
numbers_of_cargo_today = int(input(f"Моля въведете броят поръчки за тур {tour_number}: " ))
#Създаваме променлива, която сумира събраните товари
count_of_cargos = 0
#Създаваме и списък на клиентите, които ще бъдат обслужени за деня
client_list = []
#Създаваме и цикъл, който на база броят товари събира информация за всеки един от тях по отделно.
for cargo in range(numbers_of_cargo_today):
    client_name = input("Моля въведете името на получателя на пратката: ")
    lenght_of_current_cargo = int(input("Моля въведете дължината на товара: "))
    widht_of_current_cargo = int(input("Моля въведете широчината на товара: "))
    weight_of_current_cargo = int(input("Моля въведете, колко тежи товара: "))
    #Правим изчисления на база входните данни за всеки товар и вадим от вече променените данни в хода на товаренето
    room_space -= cargo_sized(lenght_of_current_cargo, widht_of_current_cargo)
    max_overload -= weight_of_current_cargo
    
    #Правим проверка, дали товарът отговаря на изискванията за тегло и заемани празно останало пространство в камиона
    if room_space < 0: 
        room_space += cargo_sized(lenght_of_current_cargo, widht_of_current_cargo)
        print("Този товар не може да се побере в камионът, моля заменете товарът!!!")
        continue
        
    
    if max_overload < 0:
        max_overload += weight_of_current_cargo
        print("Този товар е прекалено тежък, за да се качи в камионът, моля заменете товарът!!!")
        continue
        #Ако не отговаря на горните две условия не приемаме товара и го връщаме за смяна с друг товар!
           
    print(f"Вие запълнихте {cargo_sized(lenght_of_current_cargo, widht_of_current_cargo)} пространство в камионът /"
          f"оставащо пространство в камионът {room_space}")
    count_of_cargos += 1
    client_list.append(client_name)
    #Ако товарът се събира в камиона и не води до претоварване записваме, че поръчката е приета и записваме името на клиента в списъка с клиенти за доставка!

print(f"Вие успешно запълнихте камионът, като оставащото свободно място в камионът е {room_space}")
print(f"Клиентите, които ще достави тур {tour_number} ще са следните {client_list}")
